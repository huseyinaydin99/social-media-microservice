server:
  port: 9191

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://service-postgersql:5432/AuthDB
  jpa:
    hibernate:
      ddl-auto: update